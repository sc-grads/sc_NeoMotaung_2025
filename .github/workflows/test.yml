name: Test Runner
on: workflow_dispatch
jobs:
  test:
    runs-on: [self-hosted]  # Or your specific runner label
    steps:
      - name: Confirm working
        run: echo "Success! Runner is alive at $(date)"
